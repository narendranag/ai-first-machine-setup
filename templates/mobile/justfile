# justfile — setup / test / lint / run are the contract every repo keeps.

setup:
    pnpm install

dev:
    pnpm expo start

test:
    pnpm vitest run

lint:
    pnpm biome check .

run: dev

build platform='all':
    pnpm eas build --platform {{platform}}
